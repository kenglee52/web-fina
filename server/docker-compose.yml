version: '3.8'

services:
  app:
    build: .
    image: iquri/fina-website-backend:latest
    container_name: fina-backend
    ports:
      - "8001:8001"
    env_file:
      - .env
    volumes:
      - ./file:/app/file
    restart: unless-stopped